apiVersion: backstage.io/v1alpha1
kind: Component

metadata:
  name: demo
  description: Demo Spring Boot microservice
  annotations:
    # Le dice a Backstage que genere TechDocs desde la raíz del repo
    backstage.io/techdocs-ref: dir:.
    # Habilita enlaces automáticos al código y CI de GitHub
    github.com/project-slug: EleazarBT/demo

spec:
  type: service
  lifecycle: experimental
  owner: eleazarbt
